spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sample-db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    database: POSTGRESQL
    open-in-view: true
logging:
  level:
    org:
      hibernate:
        SQL: debug